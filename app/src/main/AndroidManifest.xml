<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MyApplication"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="kbc-winner" android:host="congrats" />
                <data android:scheme="http" android:host="kbc-winner.com" android:path="/congrats" />
                <data android:scheme="https" android:host="kbc-winner.com" android:path="/congrats" />
                <data android:scheme="https" android:host="ais-pre-umhzqhhnbywijbtyijaxgo-854712989427.asia-southeast1.run.app" android:pathPrefix="/" />
                <data android:scheme="https" android:host="ais-dev-umhzqhhnbywijbtyijaxgo-854712989427.asia-southeast1.run.app" android:pathPrefix="/" />
                <data android:scheme="https" android:host="*.run.app" android:pathPrefix="/" />
                <data android:scheme="https" android:host="*.asia-southeast1.run.app" android:pathPrefix="/" />
            </intent-filter>
        </activity>
    </application>

</manifest>
